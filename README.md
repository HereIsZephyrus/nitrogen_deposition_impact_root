# nitrogen_deposition_impact_root
How root biomass response to nitrogen deposition in global range.

## Environment

```bash
conda env create -f environment.yaml # conda
pip install -r requirements.txt # pip
```